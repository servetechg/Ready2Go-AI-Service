"""Python-owned state package — `ai_*` collections in the app's `ready2go` DB
(cache, aggregate, call log). Never the app's domain docs.

TODO(Step 5): models (collection helpers + indexes), cache, aggregate, calllog.
"""

from __future__ import annotations
